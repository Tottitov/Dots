experimental-backends = true;
vsync = true;
backend = "glx";
use-damage = false;
glx-no-stencil = true;

fading = true;
fade-in-step = 0.04;
fade-out-step = 0.04;

wintypes:
{
  tooltip = { fade = true; shadow = false; opacity = 1; focus = false; full-shadow = false; };
  dock = { shadow = false; }
  dnd = { shadow = false; }
  popup_menu = { opacity = 1; }
  dropdown_menu = { opacity = 1; }
};
